# Demo repo

These are the AWS documentation example keys. They are not live.

```sh
cd demo
git init
# In the bar popup, add this directory as a watched git root, then:
git add .env
```

The canary should alarm on the staged AWS key. Enter reverse-applies only the
secret hunk; `KEEP_ME` stays staged and the working tree is untouched.
